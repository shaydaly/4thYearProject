<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_camera_recorder"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.carvis.CameraRecorder">

    <SurfaceView
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:id="@+id/carvisSurfaceView"
        android:layout_alignParentTop="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/startService"
        android:text="start"
        android:layout_below="@+id/carvisSurfaceView"
        android:layout_alignLeft="@+id/carvisSurfaceView"
        android:layout_alignStart="@+id/carvisSurfaceView" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/stopService"
        android:text="stop"
        android:layout_below="@id/startService"
        android:layout_alignParentBottom="true"
        android:layout_alignLeft="@+id/startService"
        android:layout_alignStart="@+id/startService" />

</RelativeLayout>
